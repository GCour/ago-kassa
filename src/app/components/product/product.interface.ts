export interface Product {
    id: number;
    name: string;
    category: number;
    price: number;
    stock: number;
    img: string;
}

export interface Category {
    id: number;
    title: string;
}
